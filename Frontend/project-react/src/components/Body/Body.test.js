import afficherPage from './Body'
import { render, fireEvent } from '@testing-library/react';

it('should afficherPage de Body', () => {
    //afficherPage(event, id, categoryOrArticle)
    //expect(pageUpdate).toBe(id)
})